package monete;

public interface CanSalvadanaio {
	  void addMoneta(Moneta moneta);
	    double getTotal();
	    int conteggio(Moneta moneta);
	    boolean remove(Moneta moneta);
	    int findMoneta(Moneta moneta);
	

}
